//A set is a container that holds unique values. Imagine it being a bag that allows you to add items to it but rejects items that have already been added

var bag = {"pebbles", "sarah", "Michael"};
void main() {
  bag.add("pebbles");
  print(bag);
}
